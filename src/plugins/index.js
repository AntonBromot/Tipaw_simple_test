const vision = require('vision'),
      inert = require('inert'),
      lout = require('lout'),
      hapiAuthBasic = require('hapi-auth-basic')

module.exports = [ vision, inert, lout, hapiAuthBasic ]
